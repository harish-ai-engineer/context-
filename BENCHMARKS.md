# Benchmarks

**Status: harness live, seed corpus (7 docs). Real-document corpus in progress.**

The v0.1 quality bar (see `agentcontext-v0.1-spec-and-readme.md`) requires a
public, honest benchmark. The harness and seed results are below; the corpus
grows toward 25–50 real-world documents before we treat the numbers as final.

Reproduce locally:

```bash
pip install -e ".[all,dev]" markitdown[pdf,docx]
python benchmarks/make_corpus.py     # regenerate corpus + goldens
python benchmarks/harness.py         # print the table
```

## Methodology

1. **Golden corpus:** documents with expected output correct by construction —
   each corpus file and its golden are generated from one shared definition
   (`benchmarks/make_corpus.py`). Target: 25–50 real-world documents (papers,
   invoices, reports, contracts, docs pages) with hand-verified goldens.
2. **Metrics** (`benchmarks/harness.py`):
   - **Text extraction accuracy** vs. golden output (normalized diff)
   - **Heading/structure accuracy** (correct levels and order)
   - **Table cell accuracy** (% of cells correct)
   - **Provenance accuracy** (page numbers correct on sampled blocks)
3. **Baselines:** MarkItDown and Docling, run on the same corpus with default
   settings.

## Commitments

- Numbers are published **even where we lose**. Trust is the product.
- The corpus, harness, and scoring scripts ship in this repo so anyone can
  reproduce the table.
- Every release updates this page from CI, not by hand.

<!-- BENCH:START -->

## Current results (seed corpus, 9 documents)

Generated by `python benchmarks/harness.py --update-benchmarks-md`.

**Read this honestly:** the seed corpus is synthetic and clean, so any good converter sits at or near the ceiling on text/structure/tables — parity there is expected, not a win. The differentiating column is **Provenance**: baselines score 0% because they do not track source locations at all, not because they parse badly. Real-document results (messy PDFs, scans, complex layouts) will replace these numbers as the corpus grows toward 25–50 documents.

| Parser | Text | Structure | Table cells | Provenance |
|--------|------|-----------|-------------|------------|
| **agentcontext** | 100.0% | 100.0% | 100.0% | 100.0% |
| **markitdown** | 100.0% | 100.0% | 100.0% | 0.0% |
| docling | *not installed* | | | |

<!-- BENCH:END -->
